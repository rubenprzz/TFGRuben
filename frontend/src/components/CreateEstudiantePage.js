import React, {Component} from "react";

export default class CreateEstudiantePage extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return <p>Pagina que crea estudiantes</p>
    }
}